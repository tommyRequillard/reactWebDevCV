stages:
  - build
  - test
  - predeploy
  - deploy

image: node
cache:
  paths:
    - "node_modules"
    - "dist"

build:
  stage: build
  before_script:
    - npm install
  script:
    - npm run build

test:
  stage: test
  image: sonarsource/sonar-scanner-cli:latest
  script:
    sonar-scanner

predeploy:
  stage: predeploy
  image: node:latest
  before_script:
    - npm install -g netlify-cli
  script:
    - cp netlify.toml dist/
    - cd dist
  only:
    - master

deploy:
  stage: deploy
  image: node:latest
  before_script:
    - npm install
  script:
    - npm publish
  only:
    - dist